# =============================================================================
# Room3D Viewer -- Python Dependencies
# =============================================================================
# IMPORTANT: Install PyTorch FIRST (separately) with CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# THEN install this file:
#   pip install -r requirements.txt
# =============================================================================

# Web server
flask>=3.0,<4.0
flask-cors>=4.0,<5.0

# Hunyuan3D dependencies
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# 3D mesh processing
trimesh>=4.0.0
pygltflib>=1.16.0

# Utilities
python-dotenv>=1.0.0
huggingface-hub>=0.23.0

# Optional: better logging
rich>=13.0.0
